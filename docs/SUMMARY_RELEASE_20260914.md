# 메인 서버 요약 연결 릴리스

2026-09-14. 서버 요약의 원본 일치 검증과 serve 전환을 완료했다. 프런트 summary 배포를 준비한다. 전체 성능 계획의 전 메뉴 이행·운영 하루 인수 완료를 뜻하지 않는다.

## 적용 내용

- 메인 bootstrap에 `productionDashboardViews/v1__YYYY-MM-DD` 공급자를 연결했다. 단일 root 또는 검증된 immutable chunks로 표시 모델을 받는다.
- schema/logic/date/generation/build/hash 검사 후, 자동 알림 검사 동안 변경이 없었는지 화면 설치 직전에 다시 검사한다.
- off·불완전·통신 오류·불일치일 때 기존 원본 경로로 복구한다. 권한 거부를 빈 화면 자료로 해석하지 않는다.
- 메인 방문 시 자동 알림 생성 정책과 command의 원본 서버 재확인은 유지한다. 저장에 요약 모델을 원본처럼 사용하지 않는다.
- 다른13개 메뉴의 데이터 경로는 기존 방식이며 공통 shell·수명 정리·설정 지연조회·XLSX 지연 로딩 개선만 적용돼 있다.

## 운영 검증

- backend runtime source `4c61a80`, logic `ac3cd78d47f19528409da0373f8bff9688e39e738e45eeb0060ad433fe8ae151`, Functions23개 ACTIVE.
- 날짜 사실101개 생성, 공개 모델과 독립 원본 계산 hash 일치. 원본을 두 번 읽어 조회 도중 변경이 없는 표본만 합격시켰다.
- serve 검증 시 generation368, 공개189,941bytes, 서버 원본읽기1,672건. 초기화 이후 자연 발생 설비·로그 변경의 새 generation 게시도 확인했다.
- 승인 증거 `readpath.summary-acceptance.json`. 배포 wrapper는 당일 serve 증거·동일 logic·실제64자리 hash·24시간 유효기간을 검사한다.
- 프런트43개·서버14개·서버 emulator12개 통과. 로컬 브라우저에서 요약 변조/차단 후 본문 동일·쓰기0. 요약 cold 진입의 fixture 조회21회에는 공통 상단 및 자동 알림 조회가 포함된다. 운영 네트워크 속도 측정치가 아니다.
- 초기 프런트 session 개선의 합성20표본은 cold data ready56~58% 감소, 조회53→27. 이 수치를 서버 연결 후의 추가 개선율로 재사용하지 않는다.

## 비용과 안전한 복구

사용자 목표 월 추가1만원. 서울 단가·무료분 미공제·사용자10명·공개200KB·평균 실행시간 가정으로 월9,698원 모델. 작업당 원본1,800건/쓰기100건, 운영110시도/일·원본변경250건/일 한도를 사용한다. 실제 청구액 상한은 아니며 사용량이 가정보다 많으면 달라진다.

집계 한도 도달 시 원본 조회를 사용한다. 원본변경 한도 초과 시 서버 요약 off와 재집계 필요 상태로 전환한다. 업무 저장을 막거나 원본을 삭제하지 않는다. 초기101개 수동 작업이 이전 버전의 일일 counter에 포함된 부분은 checkpoint를 확인해 projection 감사 문서와 함께1회 보정했다. 새 초기화는 별도 cap을 사용한다.

서버 상세·명령: `C:/dev/fant-inventory-dashboard/docs/PRODUCTION_DASHBOARD_RELEASE_20260914.md`.

## 배포 및 남은 인수

프런트의 검증 source·gh-pages·전체 자산 hash·운영 메뉴 확인 결과는 실제 배포 후 아래에 기록한다. 자연 변경의 전체 지연 분포, 운영 하루 비용 관측, 전 메뉴 세션 모델/게이트웨이/LRU 및 전체 업무 저장·동시성 검증은 미완료다.
